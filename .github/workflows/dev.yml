name: dev

on:
  push:
    branches: [dev]

jobs:
  Build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@main

      - name: Build container
        run: docker build . -t 5scontrol/idle_python:latest

      - name: Push container
        run: docker push 5scontrol/idle_python:latest
